export const Results = () => {
    return ( 
        <ul id="results">
            {/* $("<li/>", {text: item, class: "history","data-type": "history"}).data("item", item); */}
        </ul>
    );
};
    